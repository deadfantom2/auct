const HistoricBidders = (biddersHistory) => {
  return (
    <>
      {/* {biddersHistory.map((bidder, index) => {
        return <p key={index}>{bidder}</p>;
      })} */}
    </>
  );
};

export default HistoricBidders;
